export const metadata = {
  title: "CMC Visuals — Sports Media Coverage",
  description: "Pro photo & video coverage for athletes and teams.",
};
export default function RootLayout({ children }) {
  return (<html lang="en"><body style={{ margin: 0, background: "#0a0a0a", color: "#f5f5f5", fontFamily: "system-ui, sans-serif" }}>{children}</body></html>);
}
